# Changelog

Script changes, chapter by chapter — including **merged contributions from
annotations and pull requests, credited by name/handle** (see
`CONTRIBUTING.md` and the *How to annotate* appendix).

## Unreleased

- Converted the full script v2.8 (Sessions 1–13 + reading list) from LaTeX to
  Quarto. Every chapter now ends with a Self-check section (6–8 questions,
  section-tagged, basic/applied); answer keys live in the source-only
  `answers/` directory; Canvas quiz exports (QTI zip + CSV) are committed in
  `canvas-export/`. The chapters' original "Sketch answers" paragraphs moved
  from the chapter text into the answer keys.

- Initial Quarto scaffold: self-check question format + source-only answer
  keys, Canvas quiz export tooling, Hypothesis annotation layer behind an
  off-by-default flag, annotation guide, and contribution/co-authorship
  flow. Chapters pending conversion.
