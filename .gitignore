# Quarto build output
_site/
.quarto/
/.quarto/

# Canvas exports (canvas-export/*.qti.zip, *.csv) are generated by
# canvas-export/make_canvas_quiz.py but committed so quizzes can be
# imported without running the script. Regenerate rather than edit.
